export const goToIntroduction = (history) => {
  history.push("/intro");
};

export const goToProjects = (history) => {
  history.push("/projects");
};

export const goToContacts = (history) => {
  history.push("/contacts");
};
